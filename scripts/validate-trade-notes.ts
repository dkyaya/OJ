import { readFile } from 'node:fs/promises'; import path from 'node:path'; import { fileURLToPath } from 'node:url'; import { z } from 'zod';
async function main(){const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'); const note=await readFile(path.join(root,'Trade Ideas/2026-08-SPY-Bull-Call-Spread.md'),'utf8'); const required=['id','ticker','strategy','status','bias','planned_entry','planned_exit_before','expiration','long_strike','short_strike','net_debit','maximum_profit','maximum_loss','break_even','delta','theta','volume','open_interest','bid','ask','actual_entry','actual_exit']; const found=Object.fromEntries([...note.matchAll(/^(\w+):\s*(.+)$/gm)].map(([,k,v])=>[k,v.trim()])); z.object(Object.fromEntries(required.map(k=>[k,z.string().min(1)]))).parse(found); if(!note.includes('## Candidate structures')) throw new Error('Trade note is missing candidate structures.'); console.log('Trade-note schema validation passed.');}
void main();
