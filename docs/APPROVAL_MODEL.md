# Approval model

Cloud Draft → User submits → Automated PR → Codex reviews/corrects → Checks pass → User manually merges → Pages deploys → Supabase marks published.

Entries, exits, thesis changes, account configuration, privacy changes, and deletions remain manually approved. Profit never substitutes for process quality. OJ never executes brokerage actions.
