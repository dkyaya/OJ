import{describe,expect,it}from'vitest';import{packet}from'./packet';describe('fallback packet',()=>{it('flattens multiline user fields and includes safety metadata',()=>{const text=packet({id:'1',kind:'Create trade idea',data:{Ticker:'SPY\nunsafe heading',Missing:''},updatedAt:'2026-08-04T00:00:00Z',sync:'draft'});expect(text).toContain('Ticker: SPY unsafe heading');expect(text).toContain('Missing: TBD');expect(text).toContain('No brokerage access')})});
