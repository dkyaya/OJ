import{describe,expect,it}from'vitest';import{packet}from'./packet';describe('fallback packet',()=>{it('flattens multiline user fields and includes safety metadata',()=>{const text=packet({id:'1',kind:'Create trade idea',data:{Ticker:'DEMO\nunsafe heading',Missing:''},updatedAt:'2026-08-04T00:00:00Z',sync:'draft'});expect(text).toContain('Ticker: DEMO unsafe heading');expect(text).toContain('Missing: TBD');expect(text).toContain('No brokerage access')})});
