import{describe,expect,it}from'vitest';import{revisionState}from'./revisions';describe('revision conflicts',()=>{it('preserves divergent revisions',()=>expect(revisionState(2,3,true,true)).toBe('conflict'));it('marks newer cloud data outdated',()=>expect(revisionState(2,3,false,true)).toBe('outdated'));it('keeps equal unchanged revisions synced',()=>expect(revisionState(3,3,false,false)).toBe('synced'))});
